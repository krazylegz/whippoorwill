<h1>New category</h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>
  <div id="category_name_div">
    <label for="category_name">Name</label>
    <%= f.text_field :name %>
  </div>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', categories_path %>
